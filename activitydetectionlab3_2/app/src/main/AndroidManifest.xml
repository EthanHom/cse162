<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ethanhom.cse_162.activityrecognitionlab3">

    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Activityrecognitionlab3">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--        <service android:name=".services.DetectedActivityInentService" />-->
        <!--        <service android:name=".services.ActivityDetectionService" />-->

        <service
            android:name=".services.DetectedActivityIntentService"
            android:exported="false" />
        <service
            android:name=".services.ActivityDetectionService"
            android:enabled="true"
            android:exported="true" />

    </application>

</manifest>


    <!--        android:dataExtractionRules="@xml/data_extraction_rules"-->
    <!--        android:fullBackupContent="@xml/backup_rules"-->
    <!--    xmlns:tools="http://schemas.android.com/tools"-->
    <!--    android:exported="true">-->